namespace Common.Enum.Status
{
    public enum StatusCode
    {
        NotFound = 404,
        BadRequest = 400,
        InternalServer = 500,
        Unauthorized = 401,
        Forbidden = 403
    }
}
